import { TaskGeneralEvent, ExecutionState } from "../types"
import { AsyncQueue } from "./asyncQueue"

export function plan(prompt: string, resultQueue: AsyncQueue<TaskGeneralEvent>, state: ExecutionState) {
    
}